<?php

namespace {{ testNamespace }}\Feature;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;

class {{ modelName }}ControllerTest extends TestCase
{
    use RefreshDatabase;

    public function test_index_returns_success()
    {
        // Placeholder: add factories and assertions specific to the generated model
        $response = $this->getJson('/api/{{ modelKebabPlural }}');

        $response->assertStatus(200);
    }
}
